services:
  boids:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BOIDS_UID: ${BOIDS_UID:-1000}
        BOIDS_GID: ${BOIDS_GID:-1000}
        BOIDS_USER: ${BOIDS_USER:-jerry}

    image: boids-c-container:latest
    container_name: boids-c-container

    user: "${BOIDS_UID:-1000}:${BOIDS_GID:-1000}"

    working_dir: /workspace

    volumes:
      - ./:/workspace

    environment:
      - GH_TOKEN=${GH_TOKEN}

    stdin_open: true
    tty: true

    command: bash
